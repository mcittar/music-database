<h3><%= @band.name %><h3>

  <%= button_to 'Delete', band_url(@band), method: :delete %>
  <%= button_to 'Edit', edit_band_url(@band), method: :get %>
  <%= button_to 'Back', bands_url, method: :get %>
  <br>
  <%= button_to 'Albums', albums_url, method: :get %>
